---
import Layout from "../../layouts/Layout.astro";
const allPost = await Astro.glob("./*.md");

import PostCard from "../../components/PostCard.astro";
import trebleWinners from "../../images/blog-post-1.png";
---

<Layout title="Our Blog" />
<!-- {allPost.map((post) => <PostCard {post} />)} -->
<PostCard
  frontmatter={allPost[0].frontmatter}
  url={allPost[0].url}
  image={allPost[0].frontmatter.image.src}
/>
<style></style>
